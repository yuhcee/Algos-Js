/**
 * **67. Add Binary**
 *
 * Given two binary strings a and b, return their sum as a binary string.
 * 
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
const addBinary = (a, b) => {};
