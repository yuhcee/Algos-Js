function indexDivisibility(undefined: string): number {
  //Insert your code here

//  Take an array of length N
// check if a number in array is Unique
// A number is considered unique if:
// - It occured only once.
// - Aside the index of its first's occurence in the array, the the subsequent indices of it occurence can divide each other with no remainder
//  Output the number of unique numbers in the array

  return 1;
}


console.log(indexDivisibility('5'));
